all:
  hosts:
    production:
      ansible_host: "{{ lookup('env', 'VM_HOST') }}"
      ansible_user: "{{ lookup('env', 'VM_USER') | default('deploy', true) }}"
      ansible_ssh_private_key_file: ~/.ssh/id_rsa
